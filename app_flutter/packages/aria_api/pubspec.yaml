name: aria_api
description: Typed Dart client for the Aria music server API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.25.0
